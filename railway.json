{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get install -y nginx || true"
  },
  "deploy": {
    "startCommand": "python3 -m http.server $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
